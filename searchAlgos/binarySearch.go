package searchAlgos

import (
	"fmt"
)

// define binarySearch function and utilize the array generator to make the searchable array

func BinarySearch () int {
	// arr_size := 0
	// fmt.Scan(&arr_size)
	// fmt.Println("Array Size ", arr_size)
	// arr := utils.GenerateRandomArray(arr_size)
	// fmt.Println("Array: ", arr)

	// array must be sorted in order to implement a binary search algo, let's start with a bubble sort
	// import bubbleSort and use on the array

	// develop binary search
	// set start, end, middle
	// check target against middle
	// check if target is less than middle but greater than start, set end to middle and recalc middle
	// is greater, set start to middle and calc new mid
	// return -1
	fmt.Println("\nBinarySearch called. Implementation pending.")
	return -1
}